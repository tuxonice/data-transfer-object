<?php

namespace {{ namespace }};

use Tlab\TransferObjects\AbstractTransfer;

class {{ className }} extends {{ abstractClass }}
{
{% for property in properties %}
    private ?{{ property.type }} ${{ property.camelCaseName }} = null;
{% if not loop.last %}

{% endif %}
{% endfor %}

{% for property in properties %}
    public function get{{ property.camelCaseName|capitalize }}(): ?{{ property.type }}
    {
        return $this->{{ property.camelCaseName }};
    }

    public function set{{ property.camelCaseName|capitalize }}(?{{ property.type }} ${{ property.camelCaseName }}): self
    {
        $this->{{ property.camelCaseName }} = ${{ property.camelCaseName }};

        return $this;
    }
{% if not loop.last %}

{% endif %}
{% endfor %}
}
